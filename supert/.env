BYBIT_API_KEY=I9iTUO2iaVFK1NjNo1
BYBIT_API_SECRET=B4KBZej8Mo6j4VwNpLqZ4M64BQJIqI4opiTR

# --- Trading Parameters ---
SYMBOL="FARTCOIN/USDT:USDT"        # Trading pair (CCXT unified format, e.g., BTC/USDT:USDT, ETH/USDT:USDT)
INTERVAL="3m"                 # Candle timeframe (e.g., 1m, 5m, 15m, 1h)
LEVERAGE=20                   # Desired leverage (integer) - SET CAREFULLY!
SLEEP_SECONDS=5              # Pause between trading logic cycles (integer)

# --- Risk Management (CRITICAL - TUNE CAREFULLY!) ---
RISK_PER_TRADE_PERCENTAGE=0.002 # Risk % of equity per trade (e.g., 0.005 = 0.5%)
ATR_STOP_LOSS_MULTIPLIER=1.6    # ATR multiplier for initial Stop Loss distance
ATR_TAKE_PROFIT_MULTIPLIER=1.2  # ATR multiplier for initial Take Profit distance
MAX_ORDER_USDT_AMOUNT=10.0     # Maximum position value allowed in USDT (approximate cap)
REQUIRED_MARGIN_BUFFER=1.05     # Safety buffer for margin checks (e.g., 1.05 = requires 5% more free margin than estimated)

# --- Supertrend Indicator Parameters ---
ST_ATR_LENGTH=3
ST_MULTIPLIER=1.6               # Note: Script converts this to float for pandas_ta
CONFIRM_ST_ATR_LENGTH=3
CONFIRM_ST_MULTIPLIER=1.6       # Note: Script converts this to float for pandas_ta

# --- Volume Filter Parameters ---
VOLUME_MA_PERIOD=10
VOLUME_SPIKE_THRESHOLD=1.5          # Volume must be > this * MA for spike
REQUIRE_VOLUME_SPIKE_FOR_ENTRY=true # true or false

# --- Order Book Filter Parameters ---
ORDER_BOOK_DEPTH=20                 # How many levels deep to analyze for ratio
ORDER_BOOK_RATIO_THRESHOLD_LONG=1.2   # Min Bid/Ask ratio for long confirmation
ORDER_BOOK_RATIO_THRESHOLD_SHORT=0.8  # Max Bid/Ask ratio for short confirmation
FETCH_ORDER_BOOK_PER_CYCLE=true      # Fetch OB every cycle (more API calls)? If false, fetched only when USE_OB_CONFIRM is true and needed.
USE_OB_CONFIRM=true                   # Enable/disable OB confirmation check for entries (true or false)

# --- ATR Calculation Parameter (for SL/TP) ---
ATR_CALCULATION_PERIOD=6

# --- Termux SMS Alert Configuration ---
ENABLE_SMS_ALERTS=true               # Enable SMS alerts via Termux:API (true or false)
SMS_RECIPIENT_NUMBER="16364866381"             # Your phone number if alerts are enabled (e.g., "+11234567890"). Leave blank if disabled.
SMS_TIMEOUT_SECONDS=30                # Max time to wait for termux-sms-send command

# --- API / Internal Behavior Parameters ---
RECV_WINDOW=10000                   # API receive window (ms). Increase if you get timestamp errors.
SHALLOW_OB_FETCH_DEPTH=5            # OB levels for quick price estimate during order placement
RETRY_COUNT=3                       # Retries for certain API calls (e.g., leverage setting)
RETRY_DELAY_SECONDS=1               # Delay between retries (seconds)
API_FETCH_LIMIT_BUFFER=5            # Fetch this many extra OHLCV candles
POSITION_QTY_EPSILON=1e-9           # Small value for float comparisons (e.g., checking if position size is zero)
POST_CLOSE_DELAY_SECONDS=2          # Pause after closing a position (seconds)
POST_ENTRY_DELAY_SECONDS=1.0        # Pause after entry before placing SL/TP (seconds, can be float)
FETCH_ORDER_STATUS_RETRIES=5        # Retries for checking order fill status
FETCH_ORDER_STATUS_DELAY=0.5        # Delay between order status checks (seconds, can be float)
ENABLE_MONITOR_SLTP=false           # Enable redundant loop-based SL/TP check (NOT RECOMMENDED with exchange-native SL/TP)

# --- Logging Configuration ---
# Options: DEBUG, INFO, SUCCESS, WARNING, ERROR, CRITICAL
LOGGING_LEVEL="INFO"

