# Pyrmethus v4.5.7 - Neon Nexus Edition .env Configuration
#
# This file contains environment variables used by the Pyrmethus trading bot.
# Lines starting with '#' are comments.
# Ensure BYBIT_API_KEY and BYBIT_API_SECRET are correctly set.

# --- Core Trading Parameters ---
SYMBOL="FARTCOIN/USDT:USDT"
MARKET_TYPE="linear" # Options: linear, inverse, swap
INTERVAL="1m" # e.g., 1m, 5m, 15m, 1h, 4h, 1d

# --- Financial Parameters ---
RISK_PERCENTAGE="0.005" # Percentage of total equity to risk per trade (e.g., 0.01 for 1%)
SL_ATR_MULTIPLIER="0.25" # ATR multiplier for Stop Loss distance
TP_ATR_MULTIPLIER="0.5" # ATR multiplier for Take Profit distance (set to 0 to disable ATR-based TP)
TSL_ACTIVATION_ATR_MULTIPLIER="0.2" # ATR multiplier for Trailing Stop activation distance from entry
TRAILING_STOP_PERCENT="0.05" # Percentage of current price for Trailing Stop distance (e.g., 0.5 for 0.5%)

# --- V5 Position Stop Parameters (Bybit V5 API Specific) ---
SL_TRIGGER_BY="LastPrice" # Trigger price type for SL/TP. Options: LastPrice, MarkPrice, IndexPrice
TSL_TRIGGER_BY="LastPrice" # Trigger price type for TSL. Options: LastPrice, MarkPrice, IndexPrice
POSITION_IDX="0" # Position index for hedge mode. 0 for One-Way Mode; 1 for Buy side of Hedge Mode; 2 for Sell side of Hedge Mode.

# --- Indicator Periods ---
TREND_EMA_PERIOD="5" # Period for the long-term trend-filtering EMA
FAST_EMA_PERIOD="2" # Period for the fast EMA
SLOW_EMA_PERIOD="3" # Period for the slow EMA
STOCH_PERIOD="3" # Period for Stochastic %K calculation
STOCH_SMOOTH_K="3" # Smoothing period for Stochastic %K (to get %K)
STOCH_SMOOTH_D="3" # Smoothing period for Stochastic %D (from %K)
ATR_PERIOD="2" # Period for Average True Range (ATR)
ADX_PERIOD="3" # Period for Average Directional Index (ADX)

# --- Signal Logic Thresholds ---
STOCH_OVERSOLD_THRESHOLD="25" # Stochastic %K level to consider oversold
STOCH_OVERBOUGHT_THRESHOLD="75" # Stochastic %K level to consider overbought
TREND_FILTER_BUFFER_PERCENT="0.05" # Buffer percentage around trend EMA for price filtering
ATR_MOVE_FILTER_MULTIPLIER="0.05" # Multiplier for ATR; price move must exceed this fraction of ATR. Set to 0 to disable.
MIN_ADX_LEVEL="3" # Minimum ADX level to consider a trend strong enough for entry

# --- API Keys (REQUIRED) ---
# Replace YOUR_API_KEY_HERE and YOUR_API_SECRET_HERE with your actual Bybit API credentials.
BYBIT_API_KEY=NRrb4Biggi3sO7rKZ1
BYBIT_API_SECRET=TXztLxhYdHIcyzmN6QR2zSc2Dxj0UuQRiMzQ

# --- Operational Parameters ---
OHLCV_LIMIT="200" # Number of OHLCV candles to fetch for indicators
LOOP_SLEEP_SECONDS="0.5" # Time in seconds to wait between trading cycles
ORDER_CHECK_DELAY_SECONDS="1" # Short delay after placing an order before re-checking status
ORDER_FILL_TIMEOUT_SECONDS="5" # Max time to wait for order fill verification (implicitly via retries)
MAX_FETCH_RETRIES="3" # Maximum number of retries for API calls
RETRY_DELAY_SECONDS="2" # Delay in seconds between retries for API calls
TRADE_ONLY_WITH_TREND="false" # If true, only trade in direction of trend_ema. Options: true, false

# --- Journaling ---
JOURNAL_FILE_PATH="pyrmethus_trading_journal.csv" # Path to the trading journal CSV file
ENABLE_JOURNALING="true" # Enable or disable trade journaling. Options: true, false

# --- Logging ---
LOG_LEVEL="INFO" # Logging level. Options: DEBUG, INFO, TRADE, WARNING, ERROR, CRITICAL

# --- Testnet (Optional) ---
# Set to "true" to use Bybit's testnet. Ensure API keys are for testnet.
USE_BYBIT_TESTNET="false" # Options: true, false
