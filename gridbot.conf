# Bybit API Credentials (Use Testnet keys for testing!)
BYBIT_API_KEY=I9iTUO2iaVFK1NjNo1
BYBIT_API_SECRET=B4KBZej8Mo6j4VwNpLqZ4M64BQJIqI4opiTR
# Trading Parameters
SYMBOL="TRUMPUSDT"         # Use V5 Symbol format (e.g., XLMUSDT, TRUMPUSDT)
GRID_LEVELS=7              # Number of buy/sell levels on each side
GRID_INTERVAL=80          # Base grid interval (price difference, e.g., USDT) - Dynamic Interval will adjust from this
ORDER_SIZE=0.1           # Size of each grid order (in base currency, e.g., BTC)
LEVERAGE=50                 # Trading leverage

# Market Analysis Thresholds
MIN_24H_TURNOVER=500000    # Minimum 24h Turnover (quote currency) to consider market active
MAX_SPREAD_PERCENT=0.1       # Maximum allowed bid-ask spread (%) to place orders

# Grid Risk Management
GRID_TOTAL_PROFIT_TARGET_USD=2  # Close grid if total realized PNL reaches this (USD)
GRID_TOTAL_LOSS_LIMIT_USD=2     # Close grid if unrealized PNL drops below this (negative USD)
MAX_OPEN_ORDERS=20              # Maximum number of active grid orders

# Bot Settings
CHECK_INTERVAL_SECONDS=30        # How often to check orders/market (seconds)
LOG_FILE="gridbot.log"           # Log file name
PAPER_TRADING_MODE="true"        # Set to "true" for paper trading (no real orders)

# --- Notification Settings ---
SMS_NOTIFICATIONS_ENABLED="true"    # Set to "true" to enable SMS notifications
SMS_PHONE_NUMBER="+16364866381"       # Replace with your phone number including country code (e.g., +15551234567)

# Optional: API URL (Defaults to mainnet, use testnet URL if needed)
# BYBIT_API_URL="https://api-testnet.bybit.com"
