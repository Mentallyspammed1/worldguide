mkdir -p "$HOME/.zinit" && chmod g+w "$HOME/.zinit" && chown "$(id -u):$(id -g)" "$HOME/.zinit" && sh -c "$(curl -fsSL https://raw.githubusercontent.com/zdharma-continuum/zinit/HEAD/scripts/install.sh)" && source "$HOME/.zinit/bin/zinit.zsh" && zinit ice wait lucid light-mode for zinit light zdharma-continuum/fast-syntax-highlighting && zinit load zsh-users/zsh-autosuggestions && zinit load zsh-users/zsh-completions
